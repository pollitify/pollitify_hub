<p class="text-center">
<%= image_tag('pollitify_logo_small.png')%>
<%#= image_tag("pollitify3_color_banner.png")%>
<%#=image_tag('pollitify_logo_2E294E.webp', width: 215, height: 136)%>
</p>
<div id="tagline"><p class="text-center"><em><%=TAG_LINES.sample%></em></p></div>
<div class='my-5'>
  <p>
    <%=BASE_PRODUCT_NAME%> is your one stop source for <em>grass roots activism</em>.  We both list events near you as well as offering you a suite of software tools for creating and organizing political protests. If reasoning by analogy works for you, you can think of us as <%=link_to('Mobilize', 'https://mobilize.us/')%> with the difference that not only are we <strong>free</strong> but we also offer you the tools for putting on protests not just promoting an event.</p>
  </p>
</div>
<%= render 'tabs'%>
<%#= raise @events.inspect %>
<%= render 'tab_content', events: @events%>


<div id="suggest_button">
<p class="text-center">
<%=primary_action_button("Suggest an Event / Add Your Event", suggest_event_path)%>
<br/>
</p>
</div>
<div id="search_form" class="mt-3 d-none">
  <%= render 'index_search_form'%>
</div>
<div id="search_button">
  <p class="text-end">
    <%#= link_to("Show Search", '', class: 'btn btn-success', id: 'searchButton', 'data-turbo': "false")%>
    <button id="searchButton" class="btn btn-primary" data-turbo="false" type="button">
      Show Search
    </button>
  </div>
</div>


<% if signed_in? && current_user.admin? %>
  <%= render 'home/dashboard' %>
<% end %>

<!-- JavaScript -->
<script>
  // document.addEventListener("DOMContentLoaded", function () {
  //   var btn = document.getElementById("searchButton");
  //   var div = document.getElementById("search_form");
  //
  //   btn.addEventListener("click", function () {
  //     div.style.display = "block";
  //   });
  // });
  
  document.addEventListener("DOMContentLoaded", function () {
    var btn = document.getElementById("searchButton");
    var div = document.getElementById("search_form");

    btn.addEventListener("click", function () {
      div.classList.toggle("d-none");
    });
  });
</script>
