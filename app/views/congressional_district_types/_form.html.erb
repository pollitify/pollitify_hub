<%= form_with(model: congressional_district_type) do |form| %>
  <% if congressional_district_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(congressional_district_type.errors.count, "error") %> prohibited this congressional_district_type from being saved:</h2>

      <ul>
        <% congressional_district_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :fid, style: "display: block" %>
    <%= form.text_field :fid %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
