<h1 class="text-center">Welcome to setting up your <%=BASE_PRODUCT_NAME%> tools - 1</h1>
<h2 class="text-center">Your Organization</h1>
<%=add_spacer%>
<%= render 'progress' %>
<%=add_spacer%>
<p>We're delighted you are here.  Like you, we believe that grass roots political activism is the way to move forward in the world. <%=BASE_PRODUCT_NAME%> is <strong>group</strong> software for political organizing.  Please tell us about your organization.</p>
<%=add_spacer%>
  

<!--
  <fieldset>
    <legend>
    </legend>
  </fieldset>
-->

<%= form_with model: @organization, url: wizard_path, method: :put, class: "mt-3" do |f| %>


<fieldset>
  <legend>
    What is the name of your organization?
  </legend>
  
  <div class="mb-3">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  
</fieldset>
<%#=add_spacer%>
<fieldset>
  <legend>
    What is your city and state?
  </legend>
  
  <div class="mb-3">
    <%= f.label :city %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  
  <div class="mb-3">
    <%= f.label :state, "Please select your state",style: "display: block" %>
    <%= f.select :state_id,
      options_from_collection_for_select(@states, :id, :name, @organization.state_id),
      { prompt: '' },
      { class: 'form-select', style: 'width: 100%;' } %>
  </div>
  
</fieldset>
<%#=add_spacer%>
<fieldset>
  <legend>
    What is your website url if you have one?
  </legend>
  
  <div class="mb-3">
    <%= f.label :website_url %>
    <%= f.text_field :website_url, class: "form-control" %>
  </div>
</fieldset>

<fieldset>
  <legend>
    Does your organization use a secure chat system?
  </legend>
  
  <div class="mb-3">
    <%= f.label :secure_chat_system_id, style: "display: block" %>
    <%= f.select :state_id,
      options_from_collection_for_select(@secure_chat_systems, :id, :name, @organization.secure_chat_system_id),
      { prompt: '' },
      { class: 'form-select', style: 'width: 100%;' } %>
  </div>
  
</fieldset>
  <%= render 'nav_buttons', f: f, previous_step: @organization.previous_step, next_step: @organization.next_step %>

<%#=primary_action_button("Next", "/setup/your_domain")%>

<% end %>
