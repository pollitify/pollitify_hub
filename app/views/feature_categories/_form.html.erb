<%= form_with(model: feature_category) do |form| %>
  <% if feature_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(feature_category.errors.count, "error") %> prohibited this feature_category from being saved:</h2>

      <ul>
        <% feature_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
