<%= form_with(model: news_feed_url) do |form| %>
  <% if news_feed_url.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(news_feed_url.errors.count, "error") %> prohibited this news_feed_url from being saved:</h2>

      <ul>
        <% news_feed_url.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :rss_url, style: "display: block" %>
    <%= form.text_field :rss_url %>
  </div>

  <div>
    <%= form.label :last_checked_at, style: "display: block" %>
    <%= form.datetime_field :last_checked_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
